export const fetchPostsFromInstagram = hashtag => new Promise((resolve, reject) => {
  // comprobar que hashtag es un string no vacío
  // llamar API de Instagram
  resolve([]);
});

// const fetchPosts = hashtag => Promise.resolve({});
